# 16-Week Self-Directed Semester
## C++ / OS / ML / RL / Isaac Sim + Isaac Lab

> 목표: 16주 동안 **Modern C++(3학점) + Operating Systems(3학점) + Machine Learning/PyTorch(3학점) + Reinforcement Learning(3학점) + Isaac Sim/Isaac Lab Project(6학점)**을 하나의 연결된 커리큘럼으로 진행한다.
>
> 핵심 방향: **state/proprioception 기반 로봇 제어와 RL**에 집중한다. Vision/CNN/VLA는 이번 학기 범위에서 제외한다.

---

# 0. 전체 구조

## 학점 배분

| 과목 | 학점 | 핵심 목표 |
|---|---:|---|
| Modern C++ | 3 | 문법 → lifetime/ownership → STL → concurrency |
| OS / Systems | 3 | process → scheduling → memory → thread/synchronization |
| ML / PyTorch | 3 | 확률/고전 ML → PyTorch → MLP/training loop |
| Reinforcement Learning | 3 | MDP → value/policy → policy gradient → PPO |
| Isaac Sim / Isaac Lab | 6 | robot/physics → DirectRLEnv → PPO → randomization |

총 **18학점 상당**으로 본다.

## 고정 스택

```text
Robot / scene authoring : Isaac Sim GUI + USD
Physics                 : Isaac Sim PhysX
RL environment          : Isaac Lab DirectRLEnv
Baseline RL             : RSL-RL + PPO
Custom RL               : 직접 구현한 PyTorch PPO
Observation             : joint state / IMU / contact / target
Vision                   : 이번 학기 제외
```

## 전체 프로젝트 진행축

```text
Isaac Sim robot / physics
        ↓
PD baseline
        ↓
deterministic disturbance
        ↓
Isaac Lab DirectRLEnv
        ↓
RSL-RL PPO baseline
        ↓
Custom PyTorch PPO
        ↓
Custom robot RL
        ↓
Domain randomization
        ↓
PD vs RL benchmark
```

---

# 1. 과목별 진도

# A. Modern C++ — 3학점

주교재: **LearnCpp**  
https://www.learncpp.com/

레퍼런스: **cppreference**  
https://en.cppreference.com/

## 목표

- multi-file C++ 프로그램 작성
- reference / pointer / const / lifetime 설명
- class와 RAII 사용
- copy / move semantics 이해
- STL container / iterator / algorithm 활용
- `std::thread`, mutex, condition variable를 이용한 작은 concurrent program 작성

## 주차별 진도

| 주 | 진도 |
|---|---|
| 1 | 프로그램 구조, 변수, 함수, namespace, compilation/linking |
| 2 | header/source 분리, forward declaration, multiple translation units, CMake 기초 |
| 3 | references, pointers, arrays, `std::vector`, struct/class 입문 |
| 4 | scope, lifetime, stack/heap 개념, `const` |
| 5 | constructor, destructor, member initialization, encapsulation |
| 6 | class 사용 숙련, operator/function 복습 |
| 7 | RAII, copy constructor/assignment, move constructor/assignment |
| 8 | 중간 점검: pointer/reference/lifetime/RAII/copy-move |
| 9 | STL containers: vector, array, deque, map, unordered_map |
| 10 | iterators, algorithms, lambdas |
| 11 | templates, generic programming 기초 |
| 12 | smart pointer, ownership 설계 복습 |
| 13 | `std::thread`, thread 생성/join |
| 14 | mutex, lock, atomic 기초 |
| 15 | condition variable, producer-consumer, multi-thread control loop |
| 16 | 프로젝트 코드 정리, 부족한 LearnCpp 부분 보충 |

## 핵심 실습

```text
1. multi-file 작은 프로그램
2. 객체 생성/복사/이동/소멸 로그 실험
3. STL 기반 데이터 처리
4. producer-consumer queue
5. sensor/control/logger multi-thread 프로그램
```

---

# B. Operating Systems / Systems Programming — 3학점

주교재: **OSTEP**  
https://pages.cs.wisc.edu/~remzi/OSTEP/

Linux API 레퍼런스:  
https://man7.org/linux/man-pages/

## 목표

- process와 thread 차이 설명
- `fork`, `exec`, `wait`, `pipe` 사용
- scheduling의 목적과 trade-off 설명
- virtual memory, paging, TLB의 역할 설명
- mutex / condition variable / semaphore 사용
- timing / jitter / deadline을 측정하는 작은 시스템 프로그램 작성

## 주차별 진도

| 주 | 진도 |
|---|---|
| 1 | Introduction, Process |
| 2 | Process API, Limited Direct Execution |
| 3 | CPU Scheduling, MLFQ |
| 4 | Address Spaces, Memory API, Address Translation |
| 5 | timer/clock, periodic task 개념 |
| 6 | `clock_gettime`, `clock_nanosleep`, jitter 측정 |
| 7 | Paging, TLB, page table |
| 8 | 중간 점검: process/scheduling/virtual memory |
| 9 | Threads, Thread API |
| 10 | Locks, lock-based data structure |
| 11 | Condition Variables, Semaphores, Concurrency Bugs |
| 12 | producer-consumer 문제 구현 |
| 13 | multiprocessor scheduling / cache 관점 |
| 14 | synchronization 복습, race/deadlock 실습 |
| 15 | sensor/control/logger 구조의 multi-thread program |
| 16 | 시스템 관점에서 최종 프로젝트 분석 및 정리 |

## 필수 API

```text
fork
execve
wait / waitpid
pipe
mmap

pthread_create
pthread_join
pthread_mutex_*
pthread_cond_*
sem_*

clock_gettime
clock_nanosleep
```

File system / disk / RAID / distributed systems는 이번 학기 후순위로 둔다.

---

# C. Machine Learning / PyTorch — 3학점

주요 자료:

- Probabilistic Machine Learning  
  https://probml.github.io/pml-book/book1.html
- ISLR  
  https://www.statlearning.com/
- PyTorch Learn the Basics  
  https://docs.pytorch.org/tutorials/beginner/basics/

## 목표

- likelihood / MLE / loss의 연결 설명
- linear / logistic regression 직접 구현
- regularization / bias-variance 이해
- PCA / SVM 핵심 개념 설명
- PyTorch `nn.Module`과 training loop 직접 작성
- RL에서 사용할 MLP actor/critic을 직접 수정할 수 있는 수준 도달

## 주차별 진도

| 주 | 진도 |
|---|---|
| 1 | 확률변수, 분포, expectation, variance, likelihood, MLE |
| 2 | linear regression, least squares, MSE, gradient descent |
| 3 | logistic regression, sigmoid, cross-entropy, probabilistic classification |
| 4 | regularization, bias-variance, PCA, SVM/margin/kernel 개념 |
| 5 | PyTorch Tensor, Dataset, DataLoader, `nn.Module`, loss, optimizer |
| 6 | autograd, computational graph, backward, training loop |
| 7 | MLP, activation, initialization, batching, device |
| 8 | 중간 점검 + linear/logistic regression을 PyTorch로 재구현 |
| 9 | tensor shape 추적, batching, model save/load |
| 10 | actor/critic에 필요한 MLP 구조 연습 |
| 11 | optimizer와 learning rate, normalization 복습 |
| 12 | PPO 구현에 필요한 tensor 연산 연습 |
| 13 | custom policy network 수정 실험 |
| 14 | RL 결과 해석에 필요한 통계/plot 정리 |
| 15 | 필요 개념 보충 |
| 16 | PyTorch 코드 정리 및 재현성 확인 |

## 이번 학기 제외

- CNN 심화
- ViT
- RGB/depth perception
- image augmentation
- VLA

Vision은 후속 학기로 넘긴다.

---

# D. Reinforcement Learning — 3학점

주교재: **Sutton & Barto — Reinforcement Learning: An Introduction**  
http://incompleteideas.net/book/the-book-2nd.html

PPO 참고:  
https://spinningup.openai.com/en/latest/algorithms/ppo.html

## 목표

다음 흐름을 코드와 수식으로 연결할 수 있어야 한다.

```text
environment interaction
→ rollout
→ return / advantage
→ policy/value update
→ new rollout
```

그리고 마지막에는 **RSL-RL을 사용하지 않는 작은 PPO trainer를 PyTorch로 직접 구현한다.**

## 주차별 진도

| 주 | 진도 |
|---|---|
| 1~6 | RL 본격 진도 없음. ML/PyTorch 선행 |
| 7 | RL 시작: state, action, reward, return, policy, value |
| 8 | MDP 정리 |
| 9 | Bellman equation, Monte Carlo, TD, on/off-policy |
| 10 | policy gradient 기본, actor/critic 개념 |
| 11 | PPO 집중: advantage, GAE, ratio, clipping, value loss, entropy |
| 12 | Custom PyTorch PPO 구현 |
| 13 | custom robot 환경에서 nominal RL |
| 14 | domain randomization을 넣은 RL |
| 15 | PPO 구현/성능 병목 분석 |
| 16 | RSL-RL vs custom PPO 결과 정리 |

## 필수 구현 요소

```text
ActorCritic(nn.Module)
RolloutBuffer
GAE
PPO clipped objective
value loss
entropy bonus
minibatch update
checkpoint
```

---

# E. Isaac Sim / Isaac Lab — 6학점

Isaac Sim 문서:  
https://docs.isaacsim.omniverse.nvidia.com/latest/

Isaac Lab 3.0 문서:  
https://isaac-sim.github.io/IsaacLab/release/3.0.0/

## 목표

- Isaac Sim GUI/USD에서 articulated robot 구성
- rigid body / joint / articulation / drive 이해
- PD controller 작성
- deterministic disturbance로 baseline 평가
- Isaac Lab `DirectRLEnv` 직접 구현
- RSL-RL로 PPO baseline 확보
- custom PyTorch PPO 연결
- custom robot에서 nominal RL 후 domain randomization
- PD와 RL을 정량 비교

## 주차별 진도

| 주 | 진도 |
|---|---|
| 1 | USD / Prim / Xform / Rigid Body / Collider / Physics Scene |
| 2 | revolute/fixed/prismatic joint, drive, articulation |
| 3 | Python에서 joint state 읽기, command 보내기 |
| 4 | custom robot/environment 구조 확정, sensor/actuator interface |
| 5 | PD controller 구현 및 step response 측정 |
| 6 | deterministic force/torque/impulse disturbance |
| 7 | PD gain sweep, quantitative plot |
| 8 | 중간 프로젝트: robot + PD + disturbance benchmark |
| 9 | `Isaac-Cartpole-Direct` 해부 + `DirectRLEnv` 직접 구현 |
| 10 | batched env, `num_envs`, RSL-RL PPO baseline |
| 11 | RSL-RL 내부에서 rollout / GAE / PPO update 위치 추적 |
| 12 | custom PyTorch PPO를 Isaac Lab env에 연결 |
| 13 | custom robot `DirectRLEnv`, nominal RL |
| 14 | domain randomization + PD vs RL benchmark |
| 15 | 시스템 통합, 반복 실험, 병목/성능 측정 |
| 16 | 최종 실험, 보고서, README, 영상/그래프 정리 |

## Isaac Sim에서 반드시 이해할 것

```text
USD / Prim / Xform
Rigid Body / Collider
Revolute / Prismatic / Fixed Joint
Articulation Root
Fixed-base / Floating-base
Mass / Inertia / Center of Mass
Position / Velocity / Effort command
Contact / Friction
Joint State / IMU / Contact Sensor
```

## Isaac Lab에서 먼저 볼 것

Manager-based workflow보다 **`DirectRLEnv`를 먼저 본다.**

```text
_pre_physics_step()
_apply_action()
_get_observations()
_get_rewards()
_get_dones()
_reset_idx()
```

그리고 반드시 tensor shape를 추적한다.

```text
observations : [num_envs, obs_dim]
actions      : [num_envs, action_dim]
rewards      : [num_envs]
dones        : [num_envs]
```

## RL 연결 순서

```text
Isaac-Cartpole-Direct 해부
        ↓
내 Cartpole DirectRLEnv 직접 작성
        ↓
RSL-RL PPO로 env 검증
        ↓
RSL-RL PPO 내부 구조 읽기
        ↓
Custom PyTorch PPO 연결
        ↓
Custom robot RL
```

RSL-RL은 **baseline / 정답 구현**으로 사용한다.  
최종 목표는 RSL-RL API 암기가 아니라 **Isaac Lab의 batched tensor에 직접 작성한 PyTorch RL 코드를 연결하는 것**이다.

## 병렬 학습 실험

```text
num_envs = 16 / 64 / 256 / 512 / ...
```

측정:

- iteration time
- samples/sec
- GPU utilization
- VRAM
- visualizer on/off

동일한 총 environment step을 비교할 때는:

```text
num_envs × num_steps_per_env × iterations
```

를 동일하게 맞춘다.

## Domain Randomization 순서

```text
nominal
↓
deterministic disturbance
↓
disturbance magnitude/direction randomization
↓
payload mass / center of mass
↓
friction
↓
sensor noise
↓
actuator gain
↓
delay
```

한 번에 여러 randomization을 넣지 않는다.

---

# 2. 16주 통합 일정

| 주 | 핵심 프로젝트 마일스톤 |
|---|---|
| 1 | Isaac Sim 기본 scene + C++/OS/ML 시작 |
| 2 | articulated mechanism 제작 |
| 3 | Python에서 robot state/action 제어 |
| 4 | custom robot/environment 확정 |
| 5 | PD baseline |
| 6 | deterministic disturbance |
| 7 | PD parameter sweep |
| 8 | 중간 프로젝트 완료 |
| 9 | DirectRLEnv Cartpole 직접 구현 |
| 10 | RSL-RL PPO baseline |
| 11 | PPO/RSL-RL 내부 해부 |
| 12 | Custom PyTorch PPO |
| 13 | Custom robot nominal RL |
| 14 | Domain randomization + PD vs RL |
| 15 | 시스템 통합 / 반복 실험 |
| 16 | 최종 보고서 / 재현성 정리 |

---

# 3. 중간 프로젝트

## 요구사항

- custom articulated robot/environment
- joint state / IMU 등 state 기반 observation
- actuator interface
- PD controller
- deterministic disturbance
- quantitative metric
- plot

## 평가 지표 예시

```text
rise time
overshoot
settling time
steady-state error
task/orientation RMSE
maximum deviation
control effort
failure rate
```

---

# 4. 최종 프로젝트

## 비교 대상

```text
PD baseline
RSL-RL PPO
Custom PyTorch PPO
Randomized RL
```

## 테스트 조건

| Test | PD | RSL-RL | Custom PPO | Randomized RL |
|---|---:|---:|---:|---:|
| Nominal | | | | |
| Small disturbance | | | | |
| Large disturbance | | | | |
| Payload variation | | | | |
| Noise | | | | |
| Actuator variation | | | | |

RL이 PD보다 반드시 좋아야 하는 것은 아니다.  
중요한 것은 **동일 조건에서 재현 가능하게 비교하고 실패 원인을 설명하는 것**이다.

---

# 5. 권장 주간 시간

| 영역 | 1~4주 | 5~8주 | 9~12주 | 13~16주 |
|---|---:|---:|---:|---:|
| C++ | 6h | 5h | 4h | 3h |
| OS | 5h | 5h | 5h | 4h |
| ML | 7h | 7h | 4h | 3h |
| RL | 0h | 2h | 7h | 6h |
| Isaac | 10h | 10h | 11h | 14h |
| 합계 | 28h | 29h | 31h | 30h |

시간보다 **주차별 결과물**을 우선한다.

---

# 6. 진행 원칙

1. **Baseline이 먼저다.** PD든 RL이든 nominal condition에서 먼저 성공시킨다.
2. **DirectRLEnv를 먼저 배운다.** Manager-based environment는 필요해진 뒤 배운다.
3. **RSL-RL은 baseline이다.** 최종 목표는 custom PyTorch PPO까지 내려가는 것이다.
4. **Vision은 이번 학기에 넣지 않는다.** state/proprioception 기반 control에 집중한다.
5. **Randomization은 하나씩 넣는다.** 문제가 생겼을 때 원인을 분리할 수 있어야 한다.
6. **같은 총 sample 수로 비교한다.** 병렬 env 수만 바꾼 실험에서 총 environment steps를 통제한다.
7. **튜토리얼 완료보다 재구현 능력을 본다.**

> 문서를 닫고도 내가 이 구조를 다시 만들 수 있는가?

---

# 7. 최종 결과물

```text
C++ exercises / concurrency project
OS process / memory / synchronization exercises
ML/PyTorch exercises
Isaac Sim custom robot
PD controller
deterministic disturbance benchmark
Isaac Lab DirectRLEnv
RSL-RL PPO baseline
Custom PyTorch PPO trainer
Custom robot RL policy
Domain randomization experiments
PD vs RL benchmark
plots / logs / checkpoints
final report
README
```
